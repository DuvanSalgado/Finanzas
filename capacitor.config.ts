import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.finanzas',
  appName: 'finanzas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
